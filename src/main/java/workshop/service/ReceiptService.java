package workshop.service;

//import workshop.elements.Receipt;

import javax.persistence.EntityManager;
import javax.persistence.Persistence;
import javax.persistence.TypedQuery;
import java.util.List;

public class ReceiptService {
//    private EntityManager em = Persistence.createEntityManagerFactory("CALIBRE").createEntityManager();
//
//    public Receipt add(Receipt receipt){
//        em.getTransaction().begin();
//        Receipt receiptFromDB = em.merge(receipt);
//        em.getTransaction().commit();
//        return receiptFromDB;
//    }
//
//    public void delete(long id){
//        em.getTransaction().begin();
//        em.remove(get(id));
//        em.getTransaction().commit();
//    }
//
//    public Receipt get(long id){
//        return em.find(Receipt.class, id);
//    }
//
//    public void update(Receipt receipt){
//        em.getTransaction().begin();
//        em.merge(receipt);
//        em.getTransaction().commit();
//    }
//
//    public List<Receipt> getAll(){
//        TypedQuery<Receipt> namedQuery = em.createNamedQuery("Receipt.getAll", Receipt.class);
//        return namedQuery.getResultList();
//    }

}
